# ADC-USB Sample

This example reads the analog voltage from the ADC input (`PC09`) and outputs the measured value over the USB serial interface.

After flashing the program to the Baochip board, open a serial terminal (such as **PuTTY**, **minicom**, or the serial monitor in **VS Code**) and connect to the device.
The ADC reading will be printed once per second.
